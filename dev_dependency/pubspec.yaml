name: dev_dependency
description: "Shared Flutter dev/test dependencies - re-exports test packages for use across monorepo"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

  bloc_test: ^10.0.0
  mocktail: ^1.0.4

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
