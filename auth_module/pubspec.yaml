name: auth_module
description: Auth module - handles all authentication (login, logout, token management)
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.11.0"

dependencies:
  flutter:
    sdk: flutter
  dependency:
    path: ../dependency
  common_ui:
    path: ../common_ui
  app_route:
    path: ../app_route

dev_dependencies:
  flutter_lints: ^6.0.0
  dev_dependency:
    path: ../dev_dependency
  test_dependency:
    path: ../test_dependency

flutter:
  uses-material-design: true
