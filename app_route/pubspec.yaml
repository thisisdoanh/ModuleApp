name: app_route
description: Centralized route constants and navigation utilities for the monorepo
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.0
  flutter: ">=3.11.0"

dependencies:
  flutter:
    sdk: flutter
  dependency:
    path: ../dependency

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
